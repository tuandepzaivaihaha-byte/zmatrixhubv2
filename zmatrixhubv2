repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local VIM = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local char, hrp, humanoid

local function setup()
    char = player.Character or player.CharacterAdded:Wait()
    hrp = char:WaitForChild("HumanoidRootPart")
    humanoid = char:WaitForChild("Humanoid")
end
setup()
player.CharacterAdded:Connect(setup)

local questData = {
    {Level = 1, NPC = "Luffy", Mob = "Bandit", Pos = Vector3.new(-10, 15, 20)},
    {Level = 30, NPC = "Marine", Mob = "Marine Recruit", Pos = Vector3.new(1300, 20, -650)},
    -- ... các dữ liệu khác giữ nguyên
}

local autoFarm = true
local targetMob = nil

-- Tối ưu Noclip: Chỉ chạy 1 lần hoặc dùng vòng lặp nhẹ
RunService.Stepped:Connect(function()
    if autoFarm and targetMob then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide then
                part.CanCollide = false
            end
        end
        
        if targetMob:FindFirstChild("HumanoidRootPart") then
            local mhrp = targetMob.HumanoidRootPart
            hrp.Velocity = Vector3.new(0,0,0) -- Triệt tiêu lực đẩy
            -- Đứng trên đầu Mob một chút để tránh bị đánh trúng
            hrp.CFrame = mhrp.CFrame * CFrame.new(0, 5, 0) * CFrame.Angles(math.rad(-90), 0, 0)
        end
    end
end)

-- Click chuột (Tấn công)
task.spawn(function()
    while true do
        if autoFarm and targetMob and targetMob:FindFirstChild("
